linter:
  ktlint:
    cli:
      patterns:
        - "src/**/*.kt"
        - "!src/**/*Test.kt"
      ruleset: []
        # NOTE: No way to test `ruleset` easily.
        # - "https://example.com/ktlint/custom/ruleset.jar"
      disabled_rules:
        - "no-wildcard-imports"
        - "indent"
        - "experimental:indent"
      experimental: true
