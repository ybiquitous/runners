#!/usr/bin/env ruby

require "pathname"

$LOAD_PATH << Pathname(__dir__).parent + "lib"

require "node_harness"
require "node_harness/cli"

NodeHarness::CLI.new(argv: ARGV, stdout: STDOUT, stderr: STDERR).validate_options!.run
